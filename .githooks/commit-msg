#!/usr/bin/env bash
# No AI attribution, no co-author tags, no assistant names in history.
if grep -qiE "co-authored-by|generated with|claude|anthropic" "$1"; then
  echo "commit-msg: attribution/assistant references are not allowed in commit messages." >&2
  exit 1
fi
exit 0
